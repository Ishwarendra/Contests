#include <bits/stdc++.h>
using namespace std;

#define FIO ios_base::sync_with_stdio(false), cin.tie(NULL), cout.tie(0);
using vi = vector<int>;
using ll = long long;

int linear_search(vector<ll> &a, int X){}


void solve()
{
    int n, q;
    cin >> n >> q;
    vector<ll> a(n);

    for(ll &x : a) cin >> x;

    while(q--)
    {
        ll x; cin >> x;  

        int swaps = 0;
        int left = 0, right = n - 1;
        while(left <= right)
        {
            if ()
        }    
    }   
}

int main()
{
    int t = 1;
    cin >> t;
    while(t--)
          solve();
    return 0;
}