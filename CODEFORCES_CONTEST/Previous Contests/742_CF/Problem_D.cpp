#include <bits/stdc++.h>
using namespace std;

#define FIO ios_base::sync_with_stdio(false), cin.tie(NULL), cout.tie(0);
using vi = vector<int>;
using ll = long long;

bool is_power_of_10()
{
    
}

void solve()
{
    ll s;
    int n;
    cin >> s >> n;
    if (n == 1)
    {
        cout << 1 << "\n";
        return;
    }
}

int main()
{
    FIO int t;
    cin >> t;
    while (t--)
        solve();
    return 0;
}