#include <bits/stdc++.h>
using namespace std;

#define FIO ios_base::sync_with_stdio(false), cin.tie(NULL), cout.tie(0);
using vi = vector<int>;
using ll = long long;

void common_prefixes()
{
    int n; cin >> n;
    vi a(n);
    for(int i = 0; i < n; i++)
        cin >> a[i];
    
}       

int main()
{
    return 0;
}